<!--
Conventions are in CONTRIBUTING.md and checked by ./scripts/check-conventions.sh.
-->

## Problem

<!-- Number, title and link, e.g. 1. Two Sum — https://leetcode.com/problems/two-sum/ -->

## What this changes

<!-- New solution, a fix to an existing one, or extra checks. If it is a fix, say what was
     wrong and which input showed it. -->

## Approach

<!-- One or two sentences, plus time and space complexity. -->

## Checklist

- [ ] The file is at `solutions/<difficulty>/<NNNN>-<title-slug>.cpp` and opens with the
      problem header
- [ ] The `Solution` class is unchanged from what LeetCode expects
- [ ] `main()` covers the examples from the statement and the edge cases the constraints allow
- [ ] `./scripts/check-conventions.sh` passes
- [ ] The solution compiles and every check prints `PASS`
